import { PhoneStylePipe } from './phone-style.pipe';

describe('PhoneStylePipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneStylePipe();
    expect(pipe).toBeTruthy();
  });
});
